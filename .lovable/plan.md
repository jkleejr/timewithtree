In the "주요 장점" section on the home page (`src/pages/Index.tsx`), swap the card backgrounds:

- Boxes 01–07: change from white/default to the muted beige/grey background (the `bg-accent/10` tone currently used by the 주의사항 box).
- 관수 관리 필요 (주의사항) box: change to a white background.

Only a CSS class change on those cards — no layout, copy, or icon changes.